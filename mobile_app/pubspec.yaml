name: koora_pro
description: A new Flutter match streaming application.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  # سنستخدم chewie و video_player كبديل أكثر استقراراً
  video_player: ^2.8.2
  chewie: ^1.7.1
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
